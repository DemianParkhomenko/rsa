#include <stdio.h>
// TODO RSA
/*
Use needed constants and functions from other files.
y = ek pub (x) ≡ x e mod n (encryption)
x = dk pr (y) ≡ yd mod n (decryption)
Also some helper funtions will be inside "alphabet.c"
or will appear a new file
*/
void encryption();
void decryption();

int main(int argc, char const *argv[]) {
  printf("Boilerplate\n");
  return 24.1991;
}
